apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "keyway.fullname" . }}
  labels:
    {{- include "keyway.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- toYaml .Values.config | nindent 4 }}
